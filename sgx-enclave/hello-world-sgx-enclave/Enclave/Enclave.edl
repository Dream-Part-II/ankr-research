enclave {
	trusted {
        /* define ECALLs here. */
        public int hello_world_enclave(void);
    };

    untrusted {
        /* define OCALLs here. */
        void ocall_print([in, string]const char* str);
    };
};